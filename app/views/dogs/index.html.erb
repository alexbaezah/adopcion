<% @dogs.each do |dog| %>
    <%= dog.name %>
    <%= dog.content %>
    <% dog.adopted %>
    <% if dog.adopted === true %>
        <%= "adoptado" %>
    <%else %>
        <%= "en adopción" %> 
    <% end %>
    <%= link_to "eliminar", dog, method: :delete, data: {confirm: "Seguro que quieres eliminar?"} %>
<% end %>